name: gallery-app/frontend
type: static-website
build:
  using: node
  commands:
  - npm install
  - npm run generate
  output: dist
requires:
  api: ../csharp
  storage: ../storage
configuration:
- file: dist/settings.js