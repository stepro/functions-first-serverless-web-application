name: todoApp
components:
  myfrontend:
    type: web.server
    requires:
      env:
        MONGO_CONNECTION_STRING: $(mydata.connectionString)
  mydata:
    type: mymongo
---
name: mymongo
type: data.mongodb