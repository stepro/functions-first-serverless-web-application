export declare abstract class ServiceBase<T extends Service<T>> implements ServiceProperties, EventTarget<T> {
    readonly name: string;
    readonly type: string;
    capture(type: string, listener: EventHandlerOrObject<T, Event>): void;
    once(type: string, listener: EventHandlerOrObject<T, Event>): void;
    on(type: string, listener: EventHandlerOrObject<T, Event>): void;
    off(type: string, listener: EventHandlerOrObject<T, Event>, capture?: boolean): void;
}
